package com.example.drinkwatch.ui.theme

import androidx.compose.ui.unit.dp

object Dimens {
    /** Horizontal padding applied to screen-level content containers. */
    val ScreenHorizontalPadding = 20.dp

    /** Horizontal padding applied to dialogs, detail screens, etc. */
    val DetailHorizontalPadding = 26.dp

    /** Vertical padding for list/tab screens (search bars, list content insets). */
    val ScreenVerticalPadding = 16.dp

    /** Vertical padding for detail and settings screens. */
    val DetailVerticalPadding = 32.dp

    /** All-side padding for empty-state placeholder content. */
    val EmptyStatePadding = 32.dp

    /** Bottom padding to clear floating action buttons. */
    val FabClearance = 80.dp
}
